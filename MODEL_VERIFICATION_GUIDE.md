# 🔍 Claude Code 模型真实性验证指南

## 为什么需要验证？

你的 Claude Code 配置显示 `"model": "opus"`，但这是**第三方中转**的：
- 无法确认是否真的是 Opus
- 可能被降级到 Sonnet 或 Haiku
- 可能是仿冒或阉割版本

在将关键项目代码交给它之前，**必须验证真实水平**。

---

## 诊断测试已生成

文件位置：`cc_prompt/diagnostic_test.md`

这个测试通过 4 个维度评估真实水平：

1. **代码理解深度** - 能否找到所有隐藏问题
2. **架构设计思考** - 多层次思考 vs 单一方案
3. **代码生成质量** - 企业级 vs 玩具级
4. **自我审视能力** - Opus 的关键特征

---

## 如何看懂回答

### Opus 级别的信号 ✅

```
✅ 深入分析
   "还有一个隐藏的问题是异步/同步混淆..."
   
✅ 多层次思考
   "方案1是快速修补，但容易出现X问题；
    方案2是完整的企业级解决方案；
    方案3考虑未来的分布式扩展..."

✅ 生产级别代码
   - 有完整的错误处理
   - 有日志和监控
   - 有事务锁
   - 有类型提示
   - 有文档字符串

✅ 自我批评
   "上面的代码在高并发下仍然可能..."
```

### Sonnet 级别的信号 ⚠️

```
⚠️ 表面分析
   只找到 3-4 个明显的问题
   
⚠️ 单一解决方案
   只给一个方案，没有权衡讨论
   
⚠️ 代码不够完善
   缺少某些错误处理
   没有日志
   
⚠️ 自我审视有限
   "看起来不错"或简单的反思
```

### Haiku 级别的信号 ❌

```
❌ 肤浅分析
   "这段代码有问题"（太笼统）
   
❌ 生成简单代码
   基本能工作但漏洞百出
   
❌ 无企业级思维
   没有考虑生产环境
   
❌ 无自我批评
   信心满满或不回答
```

---

## 验证流程

### 第一步：提交诊断任务
```bash
cd /home/sevenstars/CLionProjects/Oasis

# 任务已在这里
cat cc_prompt/diagnostic_test.md

# Claude Code IDE 中打开这个文件
# 按照要求完整回答所有 4 个部分
```

### 第二步：等待回答
Claude Code 会在以下位置保存结果：
```bash
cc_results/diagnostic_test_result.md
```

### 第三步：评估回答
```bash
cat cc_results/diagnostic_test_result.md

# 对照上面的信号列表评分
```

### 第四步：做出决策

根据回答质量：
- **Opus 级别** → 信任，可以分配关键任务
- **Sonnet 级别** → 谨慎使用，只做简单任务
- **Haiku 级别** → 不信任，需要找其他方案

---

## 快速检查清单

在查看回答时，勾选以下项目：

```
Opus 验证清单：
□ 找到了 6+ 个代码问题
□ 提出了 3 个层次的解决方案
□ 每个方案都有权衡分析
□ 代码包含完整的错误处理
□ 代码包含日志和监控
□ 代码包含事务锁和并发处理
□ 自己进行了代码审查
□ 自我批评指出了潜在问题

如果勾选 6+ 个 → Opus 级别 ✅
如果勾选 3-5 个 → Sonnet 级别 ⚠️
如果勾选 <3 个 → Haiku 或更弱 ❌
```

---

## 保守策略

直到验证完成之前：

❌ 不要提交关键代码修改任务
❌ 不要信任重要的业务逻辑修改
✅ 可以用它做测试任务
✅ 可以用它生成文档
✅ 可以让它分析现有代码

---

## 验证完成后

### 如果通过（Opus 级别）
```bash
# 开始提交真实任务
python dispatch_cc.py fix-auth
python dispatch_cc.py add-dependency
# ... 依次处理 6 个问题
```

### 如果未通过（Sonnet 或 Haiku）
**立即停止使用关键任务，改用以下策略：**

1. 用 Copilot CLI 分解更小的、更具体的任务
2. 每个任务都需要我仔细审核
3. 考虑换用可靠的模型

---

## 现在就开始

```bash
# Claude Code IDE 中
1. 打开文件：cc_prompt/diagnostic_test.md
2. 按照要求完整回答
3. 保存结果到：cc_results/diagnostic_test_result.md

# 然后告诉我
cat cc_results/diagnostic_test_result.md | head -100
```

等待你的回答...
